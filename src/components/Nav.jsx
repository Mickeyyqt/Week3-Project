const Nav = () => {
  return (
    <nav className="nav">
      <h2 className="nav-title">Favourite</h2>
      <button>
        Cart<span>(1)</span>
      </button>
    </nav>
  );
};

export default Nav;
